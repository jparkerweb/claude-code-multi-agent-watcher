{
    "name": "claude-code-multi-agent-watcher",
    "version": "1.6.0",
    "scripts": {
        "install:all": "cd apps/server && bun install && cd ../client && npm install",
        "kill": "npx kill-port 7069 && npx kill-port 7070",
        "start": "npx concurrently \"cd apps/server && bun src/index.ts\" \"cd apps/client && npm run dev\"",
        "dev": "npm run start"
    },
    "devDependencies": {
        "concurrently": "^8.2.2"
    }
}